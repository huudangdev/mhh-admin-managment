{
  "host": "${process.env.HOST || '0.0.0.0'}",
  "port": "${process.env.PORT || 8000}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
